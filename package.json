{
  "name": "@tichif-ticketing/common",
  "version": "1.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "yarn run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && yarn run build &&npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.6.2"
  }
}
